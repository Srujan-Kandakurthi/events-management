[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Unknown routes → index.html (Netlify serves existing pages first).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
