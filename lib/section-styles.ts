/** Gold eyebrow label — synced across home sections (Alchemy reference). */
export const sectionEyebrowClass =
  "font-label-lg text-[10px] font-semibold tracking-[0.2em] text-secondary-fixed uppercase sm:text-xs sm:tracking-[0.22em]";

/** Section main heading — mobile-first, scales up on sm/md. */
export const sectionTitleClass =
  "mt-2 font-headline-md text-2xl leading-tight font-medium text-on-surface sm:mt-3 sm:text-3xl sm:leading-snug md:text-headline-lg";

/** Vertical section padding — tighter on mobile. */
export const sectionPaddingClass = "py-10 md:py-24";

/** Space between section header and main content. */
export const sectionContentGapClass = "mt-6 md:mt-12";
